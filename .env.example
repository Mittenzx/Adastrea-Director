# LLM Provider Configuration
# Choose between Google Gemini (default, recommended), OpenAI (legacy), or OpenRouter (unified gateway)
# Options: 'gemini', 'openai', or 'openrouter'
# Default: gemini (73% cheaper than GPT-3.5, excellent quality)
# LLM_PROVIDER=gemini

# Google Gemini API Configuration (Default Provider)
# Get your free API key from: https://makersuite.google.com/app/apikey
# Free tier: 1,500 requests/day (Flash), 50 requests/day (Pro)
# 
# NOTE: You can also save your API key permanently to local config:
#   - GUI: Check "Save API key for future sessions" when entering your key
#   - CLI: Run `python main.py --set-api-key gemini`
# Saved keys are stored securely in ~/.adastrea/config.json
GEMINI_KEY=your-gemini-api-key-here

# Optional: Choose Gemini model (defaults shown)
# gemini-1.5-flash: Fast, cost-effective, excellent for most tasks (default)
# gemini-1.5-pro: More powerful, better for complex planning tasks
# GEMINI_MODEL=gemini-1.5-flash

# OpenAI API Configuration (Legacy Support)
# Only required when LLM_PROVIDER=openai
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_ORGANIZATION=your-org-id

# OpenRouter API Configuration (Unified Gateway to Multiple Models)
# Only required when LLM_PROVIDER=openrouter
# Get your free API key from: https://openrouter.ai/keys
# OpenRouter provides access to hundreds of models (GPT-3.5, GPT-4, Mistral, etc.)
# Free tier available with generous limits
# OPENROUTER_API_KEY=your-openrouter-api-key-here
# OPENROUTER_MODEL=mistralai/mistral-7b-instruct:free

# Embedding Provider Configuration
# Choose between HuggingFace (local, free) or OpenAI (requires API key)
# Options: 'hf', 'huggingface', or 'openai'
# Default: hf (HuggingFace - no API key required)
# EMBEDDING_PROVIDER=hf

# HuggingFace Model Configuration (when using HuggingFace embeddings)
# Default model: all-MiniLM-L6-v2 (fast, good quality, runs locally)
# Other options: sentence-transformers/all-mpnet-base-v2 (slower but better quality)
# HUGGINGFACE_MODEL_NAME=all-MiniLM-L6-v2

# Vector Database Settings
# CHROMA_PERSIST_DIRECTORY=./chroma_db
# CHROMA_COLLECTION_NAME=adastrea_docs

# ChromaDB Telemetry Settings
# Set to 1 to disable anonymized telemetry collection by ChromaDB
# Note: Telemetry is already disabled by default in all Adastrea Director scripts
# You typically don't need to set this unless running ChromaDB directly
# Default: 0 (telemetry enabled), but Adastrea sets it to 1 (disabled)
# ANONYMIZED_TELEMETRY=1

# GitHub Integration (for game repository ingestion)
# Required only if ingesting from private repositories
# Get token from: https://github.com/settings/tokens
# GITHUB_TOKEN=your-github-token-here

# Alternative Google API Key Name (for compatibility)
# If you have GOOGLE_API_KEY instead of GEMINI_KEY, it will be used
# GOOGLE_API_KEY=your-google-api-key-here
